TEST SUITE:

Tests for the Linux cgroup (control groups) subsystem, covering both cgroup v1
and cgroup v2. The tests verify correct behavior of cgroup hierarchy management,
mount/umount semantics, and controller operations, and guard against regressions
in these areas.
